<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ServiceChip" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:drawablePadding">5dp</item>
    </style>

    <style name="ServiceChip.Default" parent="ServiceChip">
        <item name="android:background">@drawable/bg_type_chip</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:drawableTint">@color/black</item>
    </style>

    <style name="ServiceChip.Selected" parent="ServiceChip">
        <item name="android:background">@drawable/bg_type_chip_selected</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:drawableTint">@color/white</item>
    </style>
</resources>